extends: default

ignore: |
  .yamllint.yml
  .github
  custom_components

rules:
  line-length: disable
  trailing-spaces:
    level: warning
  indentation:
    level: warning
  empty-lines:
    level: warning
  document-start: disable
