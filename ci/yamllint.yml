extends: default

ignore: |
  .yamllint.yml
  .github

rules:
  line-length: disable
  trailing-spaces:
    level: warning
  indentation:
    level: warning
  empty-lines:
    level: warning
  document-start: disable
