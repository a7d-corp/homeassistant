- alias: "Turn off all lights"
  trigger:
    - platform: state
      entity_id: group.people
      from: 'home'
      to: 'not_home'
  action:
    service: light.turn_off
    entity_id:
      - group.automation_all_lights
