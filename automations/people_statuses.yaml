- alias: "Set person to just arrived"
  trigger:
    - platform: state
      entity_id: device_tracker.simon
      from: 'not_home'
      to: 'home'
    - platform: state
      entity_id: device_tracker.nikki
      from: 'not_home'
      to: 'home'
  action:
    - service: input_select.select_option
      data_template:
        entity_id: >
          {% if trigger.entity_id == 'device_tracker.simon' %}
            input_select.simon_status_dropdown
          {% else %}
            input_select.nikki_status_dropdown
          {% endif %}
        option: Just arrived

- alias: "Set person to home"
  trigger:
    - platform: state
      entity_id: input_select.simon_status_dropdown
      to: 'Just arrived'
      for:
        minutes: 5
    - platform: state
      entity_id: input_select.nikki_status_dropdown
      to: 'Just arrived'
      for:
        minutes: 5
    - platform: state
      entity_id: input_select.simon_status_dropdown
      from: 'Just left'
      to: 'Just arrived'
    - platform: state
      entity_id: input_select.nikki_status_dropdown
      from: 'Just left'
      to: 'Just arrived'
  action:
    - service: input_select.select_option
      data_template:
        entity_id: >
          {% if trigger.entity_id == 'input_select.simon_status_dropdown' %}
            input_select.simon_status_dropdown
          {% else %}
            input_select.nikki_status_dropdown
          {% endif %}
        option: Home

- alias: "Set person to just left"
  trigger:
    - platform: state
      entity_id: device_tracker.simon
      from: 'home'
      to: 'not_home'
    - platform: state
      entity_id: device_tracker.nikki
      from: 'home'
      to: 'not_home'
  action:
    - service: input_select.select_option
      data_template:
        entity_id: >
          {% if trigger.entity_id == 'device_tracker.simon' %}
            input_select.simon_status_dropdown
          {% else %}
            input_select.nikki_status_dropdown
          {% endif %}
        option: Just left

- alias: "Set person to away"
  trigger:
    - platform: state
      entity_id: input_select.simon_status_dropdown
      to: 'Just left'
      for:
        minutes: 5
    - platform: state
      entity_id: input_select.nikki_status_dropdown
      to: 'Just left'
      for:
        minutes: 5
  action:
    - service: input_select.select_option
      data_template:
        entity_id: >
          {% if trigger.entity_id == 'input_select.simon_status_dropdown' %}
            input_select.simon_status_dropdown
          {% else %}
            input_select.nikki_status_dropdown
          {% endif %}
        option: Away

- alias: "Set person to extended away"
  trigger:
    - platform: state
      entity_id: input_select.simon_status_dropdown
      to: 'Away'
      for:
        hours: 24
    - platform: state
      entity_id: input_select.nikki_status_dropdown
      to: 'Away'
      for:
        hours: 24
  action:
    - service: input_select.select_option
      data_template:
        entity_id: >
          {% if trigger.entity_id == 'input_select.simon_status_dropdown' %}
            input_select.simon_status_dropdown
          {% else %}
            input_select.nikki_status_dropdown
          {% endif %}
        option: Extended away
